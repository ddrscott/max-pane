# Work Queue

- [x] Ad and tracker blocking in web panes: a `WKContentRuleList` compiled from a configurable EasyList-format source, on every pane and popup, with a per-site off switch — [detail](web-content-blocking.md) — **WebKit's blocker, the store as the cache, and the ledger for the one decision that is a person's.** `ContentBlocker` fetches `blocking_list_url` (WebKit content-blocker JSON; several URLs joined), compiles it into a `WKContentRuleListStore` under the profile's Application Support, and writes a `meta.json` beside it naming the source, the time and the list identifiers; the next launch looks the lists up from there and only fetches when the cache is missing, from another source or a day old, with a daily timer after that. Nothing on the main thread but the callbacks; a failed fetch or compile keeps the last compiled list and says why. `RuleListSplit` cuts a list past 150 000 rules into parts of `limit − exceptions` ordinary rules each followed by *every* `ignore-previous-rules`, because an exception only reaches the rules before it in its own list — a plain cut would have left the second part blocking what the first part's exceptions allowed. Per-site off is migration 0013, a `blocking_exempt` table of registrable domains (`ContentBlocker.domain`: `www.youtube.com` → `youtube.com`, an IP is itself); `WebPaneAsks`'s `decidePolicyFor` tells the blocker each main-frame destination before the request, and the lists go on or off that view's `WKUserContentController` there. `toggleBlocking` (no key; `keys` binds it) is in the lane's ⋯ menu beside Mobile Layout and under Navigate, ticked while blocking, greyed on a terminal lane; the chrome bar wears an outlined `unblocked` chip on an exempt site, fading with the row on the same 0.12 s the zoom readout uses and cut under Reduce Motion, and a click on it turns blocking back on. `blocking` and `blocking_list_url` are `Config` rows under Web & memory. `WebContentBlockingTests` serves a page and a tracker from two loopback ports with a one-rule list in a temporary store and reads the tracker's request log (`LocalSite` now keeps one): the matching image never arrives and the other does; a `window.open` popup's view holds the *same* `WKUserContentController` object as its opener and is blocked the same; the switch let through on a reload and stopped again on the next, with the ledger write and the chip both checked. Measured outside the suite: the default list (ABP's EasyList conversion, 45 962 rules, 9.4 MB, last modified June 2024) compiles in 1.1 s into a 16 MB store and is found from the cache in 0 ms; ABP's `easylist_min` (27 576 rules, May 2025) in 2.7 s. **Not EasyList + EasyPrivacy:** no maintained WebKit-format conversion of EasyPrivacy exists to point at, and a converter from ABP filter syntax was out of scope, so the default is EasyList alone and the key takes more URLs. Not driven in the running app: no instance was launched, and the YouTube player under `PaneFullscreen` is the owner's check after installing
- [x] Web notifications: a page-world `Notification` shim over the per-origin ask, posting through `UNUserNotificationCenter`, click focuses the pane — [detail](web-notifications.md) — **WebKit has no `Notification` on macOS, so the page-world script (`WebNotifications`, every frame, `atDocumentStart`) is the API and the pane is the centre.** `permission` is synchronous, so every remembered answer for the pane's cookie jar is written into the script source as an origin → `granted`/`denied` map read off `location.origin`; a new remembered answer rewrites the script on every pane in that jar (`refreshUserScripts`, since `WKUserContentController` removes user scripts all or none) and tells the open document. `requestPermission()` goes through the same sheet as the camera (`.capture(what: "TO SEND NOTIFICATIONS")`, popups ask in their dialog), remembered as `SiteFeature.Notifications` — a new enum case and a `site_permissions` listing in the core, bindings regenerated, no migration. `WebNotificationCenter` posts through a `NotificationPosting` seam (`UNUserNotificationCenter.current()` traps in the test runner), fetches the icon with a 1.5 s deadline, keeps identifier → (pane, page object, frame) so a click focuses the pane via `bringForward` and fires `click` in the frame that made it; a `tag` replaces; nothing is posted for the focused pane in the key window of the active app. macOS is asked at the first grant. `WebNotificationTests`: two real-WebKit round trips over a recorder poster. Note: inside a MaxPane pane `MAXPANE_PROFILE=default` is inherited and `test.sh` keeps it, so the real-WebKit suites all skip; run `MAXPANE_PROFILE=tests ./scripts/test.sh` from a pane.
- [x] Print and Save as PDF for a web pane: ⌘P runs the system print panel, a `savePDF` command writes `createPDF` — [detail](web-print-pdf.md)
- [x] Two WebKit one-liners: `allowsMagnification` for trackpad pinch (tied to the persisted zoom ladder) and `upgradeKnownHostsToHTTPS` — [detail](web-one-liners.md)
- [x] A private web lane on a non-persistent data store, marked `PRIVATE`, recorded nowhere, gone on close — [detail](web-private-pane.md)
- [x] Geolocation: usage string, per-origin ask, and whatever WebKit needs to answer `navigator.geolocation` — [detail](web-geolocation.md)
- [x] Verify in a throwaway instance: passkeys / WebAuthn (likely needs the browser public-key-credential entitlement) and picture-in-picture under `PaneFullscreen` — [detail](web-verify-passkeys-pip.md) — **both fail**: WebAuthn is refused outright, and the entitlement needs Apple's approval plus a provisioning profile (a re-signed copy is SIGKILLed at launch); PiP is off because WebKit's `allowsPictureInPictureMediaPlayback` defaults to false on macOS. Each scoped as its own item
- [x] Spike: WebExtension support via `WKWebExtension` (macOS 15.4+) against the macOS 14 floor — load one real extension, measure, recommend — [detail](web-extensions.md)
- [x] An expanded gallery tile's stacked panes resize by dragging their seam, exactly as on the strip; unexpanded tiles stay handle-free — [detail](gallery-expanded-seam-drag.md)
- [x] `scripts/test.sh` forces `MAXPANE_PROFILE=tests` (and drops `MAXPANE_SOCKET`) instead of defaulting to it: a shell inside a live Max Pane pane inherits `default`, and every real-WebKit suite then silently SKIPs, so a green run proves less than it says. Print how many suites were skipped and why, and fail the run if any real-WebKit suite skipped for a reason other than an explicit opt-out
- [x] `WebPrintTests` "a write that cannot land is a failed row, not a silent nothing" hangs the whole suite in about half of runs: `WebPaneController.writePDF(to:)` into a missing directory sometimes never calls back, so `createPDF` or the file write is waiting on a completion that never comes. Find which, put a deadline on the wait so a hang is a failed row rather than a stuck process, and make the test deterministic (a fixture that forces the write to fail, not a missing directory that WebKit may or may not notice)
- [x] Picture-in-picture: turn on WebKit's macOS `allowsPictureInPictureMediaPlayback` preference (SPI via KVC, as MiniBrowser does), pin it with a test, then prove a PiP window survives eviction and lane close — [detail](web-pip-preference.md)
- [x] Passkeys: request Apple's web-browser public-key-credential capability, carry its provisioning profile through `build-app.sh` / `make-dmg.sh`, prove it on webauthn.io — [detail](web-passkeys-entitlement.md)
- [x] Carousel: when fewer than three lanes fit the strip, the focused lane centres itself with both neighbours peeking, so a click on either sliver pages one lane — [detail](carousel-centre.md) — `StripReveal.isCarousel` measures the three lanes around the focused one against the visible width; under it `ensureVisible` (every focus path) and `centred` land on the exact centre with no peek, `LaneSnap` settles exactly centred, and `LaneSnap.settle` holds a strip already resting on the focused lane. Three or more fitting is unchanged. Not driven in the running app
- [x] Follow the system light/dark mode live — chrome, terminals and web pages (`prefers-color-scheme`) — with a system/light/dark override — [detail](system-appearance.md) — **one mechanism, chosen by a probe, not by guessing.** `NSApp`'s `effectiveAppearance` KVO fires *before* AppKit pushes the change down, so a lane asked there still answers `aqua`; a view's own `effectiveAppearance` KVO fires at the right moment, on an ancestor override, and on re-attach to a window that changed while it was away. So `layerBackgroundColor` / `layerBorderColor` on `NSView` keep the dynamic colour and re-resolve on that KVO; all 51 hand conversions moved to them, and a source scan fails the suite on the next one. The app-wide KVO does only the crossfade: a `CATransition` fade per window on `Motion.lane`, none under Reduce Motion — filed under `kCATransition`, whatever key you pass. `IconImage` had the same bug at a second remove: its cache keyed on the colour's description, identical in both modes, so an icon was baked once for the life of the process; it now inks both and picks at draw time. Terminals needed nothing of ours: Ghostty's view already switches the shared controller's palette, and the test proves the grid and the screen survive two switches. Web pages needed nothing either, and that is proven in real WebKit on a page from disk: media query and `matchMedia` listener both follow two live switches, no reload; only `underPageBackgroundColor` is re-set, because WebKit copies it. `theme` (`system`/`light`/`dark`) is decoded per key and applied live by a `ConfigWatch` that catches in-place and atomic saves and a file created where there was none. An evicted page's snapshot keeps its appearance (ADR-0006, stated). Render sheets for header, bookmarks, toolbar and popups are built light and switched to dark in place, and were looked at. Not driven in the running app: a system-wide toggle would have switched the owner's own Mac
- [x] Every setting editable in a settings UI, stored as comment-preserving TOML at `$XDG_CONFIG_HOME/maxpane/config.toml` — [detail](settings-ui.md) — ⌘, (app menu and the sidebar gear) opens a `Popup` listing all 21 keys in six groups plus every `Command` under Keyboard, each with its value, its default, a line of explanation and whether it applies live or on relaunch; a changed non-live key says `$ relaunch to apply`. **The editor is in-house, not `toml_edit` over uniffi** ([ADR-0012](../decisions/0012-in-house-toml-line-editor.md)): config is read before the ledger opens, and the file is flat keys plus one `[keys]` table. `TomlDocument` keeps the file as its lines, and a write replaces one value's characters. The tests hold it byte for byte: key spelling, trailing comments, comments above, unknown keys, CRLF, and lines it refuses (dates, inline tables, multi-line strings, `[[tables]]`) are all kept exactly. "Default" removes the key rather than pinning today's value. `ConfigField` is the one snake_case ↔ Swift mapping, and a Mirror test fails on a `Config` property without a row. A bad value is skipped with its default and the reason shown on its row; a camelCase key is told its TOML name. `config.json` migrates once per profile and stays in place; a test decodes the same JSON both ways and requires equal `Config`s, and a value the JSON decoder would also have skipped becomes a comment. `XDG_CONFIG_HOME` is honoured (an empty or relative value is ignored, per spec); the default profile's file is at the top, other profiles stay under `profiles/`. `ConfigStore` owns the extended `ConfigWatch`, so a save from a text editor refreshes the open window and re-applies `theme`. Keyboard rows take a recorded chord (esc cancels), typed chords, none, or default, with `Keymap.complaints` shown on the row they name. "Open in editor" runs `FileOpen`'s editor line in a terminal lane. Render sheets for the top, editor/appearance and keyboard sections in light and dark were looked at, and caught three layout bugs: a vertical `NSBox` separator hugging a one-point height pulled the whole popup to its header; key and text rows overflowed the width (now a test); a long default cut off "on relaunch". Not driven in the running app: opening it needs a keystroke or click, which were off limits
- [x] Web popups (OAuth sign-in) open as a dialog over the window, never as a lane — [detail](web-popups-as-dialogs.md) — **proven in real WebKit, not assumed.** `WebPopupDialog` is a `Popup` (`.explicitOnly`) holding the `ChromeWebView` built from WebKit's own configuration; `PopupHandoff`, the pane's adoption path and the lane it staged are gone, and nothing about a popup reaches the ledger. `WebPopupDialogTests` serves an opener and a "provider" from two loopback ports — two origins — through a real `WebPaneController`: `width=500,height=600` is a 500 × 628 dialog centred in the window with the lane count unchanged; the provider's `postMessage` reaches the opener from its own origin and the cookie it sets is readable by the opener; its `window.close()` closes the dialog and moves ledger focus back from the lane that had it; five `_blank` opens are one dialog, one child window and no lanes, and the four replaced handles read `closed === true` to the page; with the opener's view in no window the dialog lands in the same place; a chooser opened from the popup stacks as a child and Esc takes both; a bare `window.open(url)` is a dialog and a clicked `target=_blank` a lane; the opener going to another origin closes it. The bar is the one row the page cannot draw — a Lucide lock and the host with its registrable name in contrast, amber `http://`, the port, the load hairline, ✕ — and its render sheet was looked at in light and dark. Departures are in [ADR-0013](../decisions/0013-web-popups-are-dialogs.md): a click on the origin copies it rather than ⌘C, which the page's selection owns and cannot be asked about synchronously; the dialog is `.normal` rather than `.floating`, so the file panel and the Keychain's panel are not under it; "the opener navigates away" means to another origin, because `pushState` is constant; ⌘W, ⌘R and ⌥⌘L act on the popup while page keys aimed at the opener behind it do nothing; the popup's questions have their own delegate over the same sheet, queue and one-shot reply, forwarding files and downloads to the pane, and the password fill was extracted into `PasswordFill.fill` rather than copied. Not driven: LinkedIn → Sign in with Google needs his account, and no app instance was launched
- [x] A page's full screen fills its pane; ⇧ or a second request goes to real macOS full screen — [detail](web-fullscreen-in-pane.md) — **proven in real WebKit, with WebKit's own full screen never entered.** `isElementFullscreenEnabled` is on, which was the whole of "your browser doesn't support full screen". `PaneFullscreen` is a page-world script in every frame that answers the API itself: the element goes into the top layer as a manual popover wearing `data-maxpane-fullscreen`, the page's `:fullscreen` rules are copied onto that attribute, and the top frame tells the pane, which gives the page the bars' height at once and fades the bars on `Motion.pane`. `WebFullscreenTests` serves a host and an embed from two loopback ports into the top pane of a split lane: the box inside a transformed card is exactly the web view and on top of a higher `z-index`; the page's own `:fullscreen` and `@media` `:-webkit-full-screen` rules apply; both change events fire and `fullscreenElement` names it; exit and a real Esc `NSEvent` restore its rect exactly while the page's own listener counts no Esc; the sibling pane does not move; an `<iframe>` without `allow="fullscreen"` is refused by WebKit's own policy and one with it fills the pane, unwinding from either side; a second request and ⇧ reach the kept native function, stood in for by a recorder, so `fullscreenState` stays `.notInFullscreen`; a navigation brings the bar back; in a popup the dialog's page fills, the origin bar stays, and the first Esc leaves full screen. ⌘L ⌘F ⌘D leave full screen first; Esc is still the page's. Found on the way: a popup's first `about:blank` gets no document-start scripts, and `userScripts` is a live array. Re-adding from it looped until the test process was killed, and that runaway is what filled the disk. See [ADR-0014](../decisions/0014-web-full-screen-fills-the-pane.md). Not driven: YouTube (network, ads), the display-wide full screen itself, and the running app. The owner checks ⤢ on a real video after installing
- [x] Session status tells the truth: green only while an agent is actually working (Claude title glyph, relay BLOCKED wins, no stuck rates) — [detail](session-status-accuracy.md) — **the max-merge is deleted, not fed.** `adopt` replaces each reading with the file's, and `observeLive` (no caller) is gone with it: SESSION_METRICS carries the same sixty-second `bps1` the file does, so wiring it would have bought ≤5 s at the cost of a freshness clock. `SessionTelemetry` keeps relay's verdict as `relayState`, and `state` is derived from it in one place, `AgentState.derived`: relay BLOCKED wins, then EXITED, then a spinner title (`◐◓◑◒`) is WORKING, a `✳` title is idle (DONE survives it, since "finished unwatched" is not "working"), else relay. Every surface already read `state`, so sidebar rows and chips, lane headers, gallery tiles, ⌘P and `N working` follow with nothing computing its own. The badge is a green rate only while the derived state is WORKING, otherwise a dim `idle`, tooltip included. A sidebar status change and a header's chip/badge change fade on `Motion.pane` (`Motion.fade`, nothing under Reduce Motion), and not on an age tick. `SessionStatusTests`: the 18-cell derivation table, the redraw-burst fixture (`bps1` 615, `✳`, relay working → idle and dim), the a7ab2d3b case (`◑`, relay idle, 492 B/s → WORKING and green), BLOCKED under any title, badge text and ink per state, and `adopt` decay through a disk-free `SessionRegistry(files:)`. The render sheet of all eight row states was looked at in light and dark. The old test that forbade a title from making a chip now allows WORKING only; a title never infers BLOCKED. Relay changes that would retire the title rule are in [proposals/relay-agent-state.md](../proposals/relay-agent-state.md). Not driven: the running app and his live sessions
- [x] One green family instead of Signal Orange: focus mid green, working muted, BLOCKED the brightest green with a slow pulse; no orange anywhere — [detail](green-palette.md) — **roles, not hues, and light mode gets its own greens** ([ADR-0015](../decisions/0015-one-green-family.md)). `Theme` defines `working` (`#16A34A`), `accent` (`#22C55E`), `blocked` (`#4ADE80`), `onBlocked` and `alive`, each dynamic; `flowing`, the inline green in `agentStateColor`, the sidebar's `live` literal and the palette's dot literal fold into them. The ticket's shades fail on a light ground (`#4ADE80` is 1.7:1 on white), so light keeps the order of *loudness* rather than lightness: blocked `#166534` 7.0:1, accent `#15773A` (the suggested `#15803D` was 4.4:1 on the strip), working `#4D7C5F`. `PaletteTests` holds it: accent and blocked text ≥ 4.5:1 on both grounds in both appearances, working ≥ 4.0, the word on a BLOCKED fill ≥ 4.5, the order in each mode, every role's hue green, the cursor following the accent, and a source scan for any orange literal. **The pulse** is `BlockedPulse`/`PulseLabel`: Core Animation opacity 1 → 0.6, 1.8 s a breath, eased; added on entering a window, dropped on leaving, eased back to full on unblocking, none under Reduce Motion (re-read live from the accessibility notification). Phase-locked to the clock (`timeOffset = now mod period`), because a sidebar row is rebuilt every age tick and would otherwise twitch once a second. It is on the header chip (word and `!`, both now filled), the sidebar chip (now solid, not an 18 % tint), a collapsed group's count, the sidebar footer's `N BLOCKED` (its own label now) and the status bar's attention count, which ASKING shares. ⌘P/⌘O rows use the blocked green but do not move. Kept on purpose: amber for `http://` and password warnings, red past the memory hard mark. Render sheets looked at in light and dark (headers now include blocked/working focused, sidebar rows, toolbar, confirm, ⌘O, import wizard, drop indicators). Not driven: the running app, and the pulse itself on screen (sheets render the model layer, not motion)
- [x] The LANES | GALLERY switch shows icons (`columns-3`, `layout-grid`), not words — [detail](layout-switch-icons.md)
- [x] Lane size presets s | m | xl — m is the 80-column default, s keeps those columns at 60% font, xl is double wide; web lanes too — [detail](lane-size-presets.md)
- [x] Gallery: every lane on one screen as live thumbnails, a sticky second layout beside Lanes — [detail](gallery-layout.md) — ⌥⌘G, `strip_layout` in `app_state`. A tile is the real `LaneView` under a transform: [spike M5](../spikes/05-gallery-scale.md) measured Ghostty's grid unchanged at every scale from 0.75 to 0.25 with zero resize reports, where a smaller backing re-flowed it at 13 of 14 points and saved no CPU. Its one blind spot was found by the tile test: Auto Layout rounds a pane to the *tile's* pixels (483 → 482.5 pt), so a terminal in a tile holds its strip size against rounding. Eviction measures distance from the focused lane, because "every lane visible" would rehydrate everything on entry and make nothing evictable. Docks are tiles, gather narrows the gallery, Esc and Return reach the tile — [ADR-0011](../decisions/0011-gallery-layout.md). Not yet driven in the running app: no instance was launched
- [x] Dock a lane to the left or right, overlaying or insetting the strip — [detail](docked-panes.md) — both halves merged, audio proved
- [x] The five silent web failures: dialogs, file inputs, downloads, camera/mic, HTTP auth — merged in 36be318
- [x] One `--profile` instead of four environment variables — [detail](profiles.md) — everything under profiles/, migration verified with sqlite3 .backup
- [x] Selecting from the session sidebar reveals a lane without focusing it — [detail](sidebar-select-focus.md) — focuses the row's own pane, and a split lane now marks which pane has the keyboard
- [x] ⌘O sometimes opens with no rows — [detail](omni-empty-on-open.md) — it never opened: a focused web pane swallowed ⌘O before the menu, and ⌘T ⌘Y ⌘R ⌘W ⌘B ⌘P ⌘[ ⌘] with it
- [x] A docked lane's page is laid out at the wrong width and runs off screen — [detail](dock-lane-width.md) — not a dock bug: every web pane was laid out at the chrome bar's fitting width and centred
- [x] Windowed, the traffic lights sit on top of + NEW — and on the first lane header when the sidebar is collapsed — [detail](titlebar-overlap.md) — the corner is reserved from whichever of the sidebar or the strip owns it, and given back in fullscreen
- [x] Browser chrome round 2: a failed navigation says nothing, ⌘-click loses your place — [detail](chrome-round-2.md) — items 1–5 done: failures speak, public http loads, ⌘-click opens a sibling lane, an untitled page stops wearing the last page's title
- [x] Browser chrome round 3: address-bar autocomplete, an honest find match count, a context menu macOS has no hook for — [detail](chrome-round-2.md) — all three merged; the menu was never blocked on API, both web views are ours; the zoom readout is still disputed and still needs a human at the keyboard
- [x] History round 2: search the whole corpus, substring ranking, client-side redirects, real dates — [detail](history-round-2.md) — no caps at all, a trigram index in their place (7 ms a keystroke over 112,840 pages), every redirect hop searchable and no interstitial left as a page, and rows that say `Tue 09:15`
- [x] Every shortcut configurable, with the current ones as defaults — [detail](configurable-hotkeys.md) — `keys` in the config file moves any of them; Esc, declared for years and listened for by nothing, is finally wired
- [x] `maxpane run "a | pipeline"` returns a session id, logs a lane, and produces none — the quoting is not the bug; the silence is. A shell line is refused before anything starts, because afterwards nobody can tell: pty-host is listening at 20 ms, the 127 lands 355 ms–1.1 s later
- [x] Import history from another browser, with a merge-or-replace wizard — [detail](import-history.md) — ⌥⌘Y; interleaved by re-deriving every `seq` from `last_visit_at` rather than appended, so the MRU is still today; merge is min/max in every field, which is what makes a second import a no-op; his Vivaldi measured at 108,854 pages in 15.4 s and 180 MB. `.backup` cannot read a running Chromium at all
- [x] History round 3: a view with room in it — day grouping, paging past 60 rows, the full URL, a confirmed delete, and a way to clear it — [detail](history-round-2.md) — ⇧⌘Y; the browse list orders by `last_visit_at` so a day header is true, day counts come from the ledger rather than from the rows loaded, and the confirmed delete landed in the palette too, where it was measured
- [x] Bookmarks: a store, a way to add and reach them, then import from other browsers — ⌘D and a ★ in the chrome bar keep a page and open the editor on it; the folders live in the sidebar above the sessions, which is the surface that already groups things, and ⌘O ranks a kept page above the visit of the same address by the same ranker history uses. Migration 0010, no index under it and the reason stated. The import rides in ⌥⌘Y rather than getting a second wizard: Chromium's `Bookmarks` JSON and Firefox's `moz_bookmarks`, bar onto bar, merging twice a no-op because the identity is address-plus-folder. Safari's binary plist is a stated non-goal, printed on the row
- [x] Bookmarks: nothing reorders them — `position` is insertion order and there is no drag. The eight folders arrive from an import in the source's order and can only be moved by deleting and re-keeping. `ordinal.rs` exists for exactly this and was passed over on the grounds that a bar is appended to rather than dragged through; the owner rearranging his bar is the case that says otherwise — drag a row between two rows or onto a folder, and Move Up / Move Down on the right-click menu for the case this is actually for: eight imported folders into a deliberate order, without aiming. `ordinal.rs` was reconsidered and passed over again, this time with a number: a drop writes once rather than once a frame, and renumbering a folder measures 0.28 ms at 47 rows and 2.7 ms at 1 000, against a fractional key that would also cost `TREE_SQL` the zero-padded sort the tree is read with. The index a move takes counts siblings *without* the moved row; the table's gaps count a list that still has it, and `SidebarModel.dropTarget` is the one place that converts. A gap resolves to the row below it, which is what makes the shallower level reachable at all
- [x] Passwords: macOS Keychain only, never our own store; explicit fill, then Chromium import — [detail](passwords.md) — **both halves, and the fill is measured rather than argued.** `kSecClassInternetPassword` with no service attribute of ours, so the items are shared with Safari: that makes Safari's logins need no import at all and puts "forget this password" in System Settings → Passwords, where macOS also keeps the access control. The store is the whole of it — a grep of the ledger schema, the config tree and the state directory finds no column, file or line that could hold a credential, and every log line in the new code carries an origin and an `OSStatus` and nothing else. Fill is ⌥⌘L and a `•••` in the chrome bar that appears only when the site has one saved, never on load, never callable by the page, origin-matched against `WKWebView.url` and re-matched after the Keychain panel answers, with the secret bound as a `callAsyncJavaScript` argument rather than built into a program. Driven against real WebKit: an ordinary form fills and dispatches `input`+`change`; two visible password boxes refuse; a sign-in form served from a second localhost port answers `opaque-frame` and writes nothing, and so does `sandbox="allow-scripts"`. That run also found the bug worth finding — WebKit reports `location.origin === "null"` for a `srcdoc` frame that provably *is* the page, so the strict comparison refused the one frame it should have allowed; reachability through `contentDocument` is the proof and is what the rule uses now, with the top document still compared exactly. Saving is ⇧⌘L and a checkbox on the HTTP-auth sheet — no submit sniffing, because a script that can offer to save what you typed is a script that reads what you typed. Chromium import is ⌃⌥⌘Y: `laned-core` copies `Login Data` with its journal siblings through the same `Snapshot` the history import proved, hands back **ciphertext**, and deletes the copy; the app reads `<Browser> Safe Storage` and opens each row. The ticket said AES-GCM — his 442 real rows all start `v10` and are block-aligned to 16, which is CBC, so it is CBC. The end-to-end import is the one thing not run: the Keychain panel needs a human at the keyboard
- [x] History: 1–2 character keystrokes cost ~60ms because trigrams need three — boundary-prefixed trigram queries make short needles indexable; measured fix, left out of round 2 as scope — **measured on his real profile, before and after, on one ledger in one process.** Migration 0011 adds a second indexed column of *shoulders*: at every place a match can start — the front of a field, and each position after a word boundary — a doubled marker character and the two that follow, so `d` is looked up as the trigram `\u1\u1d` and `de` as `\u1de`. Over his 108 854 imported pages, a–z as a first keystroke went from 61.5–84.9 ms (median 66.7) to 1.0–48.2 ms (median 10.8); every one of the 36 letters and digits got faster, and `gi do ma co gh ne lo ap` went from 65–102 ms to 0.5–22 ms. On the synthetic corpus `cost_of_a_keystroke` builds, `d` 56.3 → 20.3 ms and `de` 62.0 → 11.2 ms. **Nothing it returns is different**, and that is checked rather than argued: `a_one_character_needle_returns_what_the_whole_table_would` ranks the whole table in the test and demands the same rows in the same order, at offsets 0, 60, 240 and 600. It holds because the tiers are 10 000 apart and a score inside one spans under 1 000, so once the shoulder has handed back a page's worth at a tier the rows it left out were all a tier lower; below that the whole table is read, which is why a page whose only `z` is mid-word is still found. Three findings worth the round: `www.` is stripped before anything is matched, so indexing it as a shoulder made `w` the one keystroke this change made *slower* (76 → 84 ms) until it was removed; the two stages share one budget, because a first stage that is paid for and discarded is not free; and above a third of the table the shoulder is refused outright, since a row fetched by rowid costs ~2.0 µs against ~0.5 µs read in a sequential pass. The migration costs **6.4 s** on a 108 854-page ledger in one transaction — 4.7 s of which is 0009's own rebuild, measured by running its statement alone, and not the 543 ms its comment claims — and 171 → 217 MB
- [x] ⌘O splits a command on whitespace, so `yes | head` runs `yes` with literal args `|` and `head` — the same fault `maxpane run` just had, at a different door, and it produces a lane that spews rather than an error — **the doors differ on purpose, and the difference is what they are handed.** `maxpane run` gets argv, words some other shell already separated, so reading them again would be the double-evaluation bug and a shell-looking command *word* stays refused. ⌘O gets one string nothing has interpreted yet, typed at his own keyboard, and the only correct reader of a command line is a shell: it reaches `$SHELL -li -c` as a single argument, exactly as typed, with a newline and `exit $?` after it and nothing else. The newline is not cosmetic — `sleep 5 &; exit $?` is a syntax error in bash and sh, and a trailing comment eats a `;`-joined exit in all three. `TypedCommand.parse` is the whole decision and is a pure function: 11 new tests, no pane, no session. Measured against a throwaway pty-host on both spellings: the old argv (`'yes' '|' 'head'`) wrote **14.1 MB in two seconds** and was still running; the line read as a line writes 233 bytes and exits 0 in 400 ms. A bare `zsh` is still argv and still gets `--login`, because a wrapped shell is not the session leader and its lane's directory tag would freeze; `#` stays argv too, since a shell would eat the rest of the line. The LAUNCH row says `through zsh` before Return rather than after
- [x] Split down (⇧⌘D) adds a pane that never appears — [detail](split-down-invisible.md) — fixed in a4d414d
- [x] Lane size presets (Small / Medium / Extra Large) always in every lane's dropdown menu, docked lanes too, replacing Span Lane (2× Width); ⌘\ cycles s → m → xl — [detail](lane-size-menu.md)
- [x] Selecting text in a terminal copies it to the clipboard (`copy-on-select` is hard-coded on in `TerminalPaneController`); make it a `copy_on_select` setting, default off, with ⌘C still copying — [detail](copy-on-select-option.md) — **A setting, off, and proven on the real pasteboard.** `Config.copyOnSelect` (`copy_on_select`, a toggle under Terminals & sessions, default `false`) feeds `copy-on-select` in `TerminalControllerPool.makeController`, written as `true` or `false` and never left to Ghostty's default. It is read when the shared terminal controller is built, exactly as `font_name` and `font_size` are, so it applies on the next launch and the window marks it `$ relaunch to apply`; only `theme` is live. No second source: the app's only `NSPasteboard` writes are explicit Copy URL / Copy Path / Copy Session ID items, `ClickableTerminalView` overrides `mouseDown` for ⌘-click alone and writes nothing, and the library's one write is the clipboard callback Ghostty drives. `CopyOnSelectTests` selects on a real surface: off leaves a sentinel on `NSPasteboard.general` and `copy:` then replaces it with the selection; on replaces it at the selection — which is also what shows the hard-coded line was the cause. README and CHANGELOG (Changed) say so.
- [x] ⇧⌘↩ maximizes the focused pane to the visible viewport, and the same key puts it back exactly as it was (iTerm's Maximize Active Pane) — [detail](maximize-pane.md) — **an overlay, not a width; all three assumptions kept** ([ADR-0019](../decisions/0019-maximize-pane-is-an-overlay.md)). `PaneMaximizer` lifts the focused pane's view into `MaximizedPaneView` over the strip's visible window (rails, docks, sidebar, toolbar and status bar stay) and a `MaximizePlaceholderView` holds its slot, so siblings keep their heights, the lane its width and the strip its scroll offset; nothing is written. The layer eases from the pane's rect on `Motion.lane` both ways (the gallery's `moveTransforms`), at once under Reduce Motion, so the pane is laid out once per toggle. ADR-0007 as amended: a terminal gets the window's columns, and a real Ghostty surface with a recording attachment sends exactly one size up and one down, ending on the size it began with. A docked lane's pane is allowed and fills the strip's window while its dock stays. One rule for leaving (`MaximizeRule`, asked of every snapshot): focus off the pane, or a change to the strip's shape, restores first; zoom, reload, a title do not; Esc never; ⌘G restores at once; greyed out in the gallery. View › Maximize Pane / Restore Pane by state, `toggleMaximizePane` in `[keys]`. `MaximizePaneTests` drives a real `StripViewController` over a real ledger with stand-in panes (frames, 2:1 split, scroll offset and whole `StripState` equal after a round trip; the rule; the dock; the chord not taken by a real terminal view). `WebMaximizeTests`: `innerWidth` 600 → 1600 → 600, chrome bar along, full screen fills the maximized pane and leaves it maximized. Going up reflows at the *start* of the ease, not the end, and says so. Not driven: the running app, so the 0.22 s of content drawn through a non-uniform transform has not been seen on a screen
- [x] Only the focused terminal's cursor blinks (every terminal blinks today, worst in the gallery); make it a `cursor_blink` setting: `focused` by default, `always`, `never` — [detail](cursor-blink-focused-only.md) — **the hypothesis held, and Ghostty said so itself.** A fresh surface that never held first responder logs no `focus=` line at all and answers `CSI ? 1004 h` with `CSI I`: it is born believing it is focused, and the library reaches `ghostty_surface_set_focus` only from its responder overrides and window-key observers. The second candidate found no support: with the sync stubbed out, `makeFirstResponder` to a non-terminal still ends in `CSI O`, which is the library's own resign path working. `TerminalPaneController.syncSurfaceFocus` tells the surface the truth (first responder in the key window, because the cursor must agree with where the keys go, and the ledger still names a pane while ⌘O has the keyboard) at three moments: a surface built (`terminalDidAttachSurface`, so a rebuild counts), the view entering a window, and a turn after every `terminalDidChangeFocus`, since that arrives inside `becomeFirstResponder` where `window.firstResponder` is still the old answer. It also corrects the library reporting focus to a view made first responder in a window that is not key. The route is `ClickableTerminalView.tellSurface`, a direct call of the library's responder override, which is `super` plus the surface call and never touches `window.firstResponder`; the checkout is not patched. `cursor_blink` = `"focused"` (default) | `"always"` | `"never"`, a choice under Terminals & sessions through a new generic `choice` schema builder that `theme` now uses too, on relaunch like the font. `never` is `cursor-style-blink = false`; `always` keeps every surface told it is focused, mode 1004 included, and the README says so. `CursorBlinkTests`: schema, TOML, JSON, round trip, the rendered line for all three; on real surfaces through real `TerminalPaneController`s, read back by focus reporting: never-focused panes say `O` and never `I`, exactly one `I` as focus moves, none with the keyboard elsewhere or the window not key, keyboard and cursor agreeing after ⇧⌘↩ up and down, a tile hold, and a lane out of the window and back; and the library-alone case kept as a test, so a fix upstream announces itself. With the sync stubbed out five of the eight fail. Not driven: the running app, so the blink itself and the key-window return path have not been seen on a screen
- [x] Spike: one remote relay-tty lane through relaytty.com, measured — byte-exactness, latency, reconnect, and what auth looks like on the wire; go/no-go for Phase 1 — [detail](remote-relay-spike.md) — **GO, with Phase 0b in front** ([report](../spikes/07-m7-remote-relay.md)). `RelayTransport` seam under `RelaySession` (`handle` untouched): `UnixSocketTransport` is the old `connect`/`drain` moved, `WebSocketTransport` is `URLSessionWebSocketTask` with no prefix either way, text dropped, PING 10 s / zombie 45 s, 4001/1008 final, and the adapter stops reconnecting on a final close; ten `RelayTransportTests` against a Network.framework WS server. The app reaches it only through `MAXPANE_SPIKE_REMOTE`; `build/spike.app` built, not launched. Measured with `spikes/m7-remote-relay/` (the app's own transport, symlinked): 100 001 lines byte-exact on tunnel, ssh-forward and the box's Unix socket, live and replayed, equal to the ground truth; keystroke echo through the tunnel **25.2 ms p50 / 38.6 p95** (n=200) vs 19.6/37.9 over an ssh forward, 1.8/2.9 loopback WS on the box, 0.74/1.31 the box's socket, **0.50/0.63 a local socket on this Mac**; fresh attach to `SYNC` 143–221 ms with the replay clamp honoured, so `RESUME` makes pty-host's window through the tunnel; pastes of 1 023, 4 096 and 65 536 bytes whole; prompt → `SESSION_UPDATE blocked` 721 ms median (n=6) through the tunnel, none on attach, no OSC 7 from the box's bash. **Auth on the wire, as fact:** through relaytty.com `/ws/sessions/:id` handshakes and replays with no cookie, a wrong cookie, and either `?token=` (five probes, replay in hand; the server's peer is `127.0.0.1` from the tunnel client, `verifyWsAuth` returns before any header); LAN-direct all four are `HTTP/1.1 401` and only the right cookie handshakes. Reconnect (server frozen 10/60/180 s): 10 s costs a PONG; 60 and 180 s zombie at 45 s, reconnect within 0.5 s of the server's return, offsets right — but the replay is the **whole ring flagged delta**, because `tunnel-client.ts` drops a `DATA` that arrives before its local WS is open and pty-host sends no `SYNC(0)` on a missed window; detectable client-side (replay length == `SYNC`). The tunnel itself had died silently before the run started (no keepalive on either side; `ESTAB` on the box, 101 then nothing at the edge). Not measured: LAN-direct from this Mac (not on the LAN), a real Mac network drop and sleep/wake (the owner's network), a real Claude permission prompt (the box's login expired, auto mode on), the lane in the running app. Phase 0b ticket in §9: `?token=` on `/ws/sessions` and `/ws/events`, `x-relay-tunnel` on the bridge's upgrade with the cookie carried through `CLIENT_OPEN`, queue `DATA` until the local WS opens plus `SYNC(0)` before every full replay, a tunnel keepalive, `SESSION_UPDATE` on attach
- [x] Remote relay, Phase 1: servers exist; a running remote session attaches as a lane, keyed by (server, id), with migration 0015 — [detail](remote-relay-phase1.md) — **done** ([ADR-0020](../decisions/0020-a-session-belongs-to-a-server.md), README "Remote servers"). `SessionKey(server, id)` through the registry, attached/DONE, sidebar, strip and ⌘O; `pane.relay_server` (migration 0015) with `attach_remote_session`/`add_remote_pane` and `refuse_second_pane` on the pair; `SessionSource` seam (`DiskSessionSource`, `RemoteSessionSource` over `GET /api/sessions?includeExited=1` + `/ws/events`, 401 final); adapter hardened (wake PING, full-replay-as-delta replaced, refusal banner); `[[servers]]` needed parser work (`TomlDocument` learned array-of-tables, editor preserves comments); token in the Keychain via `maxpane server add NAME <startup URL>` through the control socket; `SpikeRemote` removed. Verified against the box through relaytty.com: list (5 sessions, 1 running), wrong token 401 → refused and not retried, `0368d543` attached over the WebSocket (80x40, 5.9 KB replay, Enter echoed); tunnel had died quietly again and was restarted in tmux. Not seen: the lane in the running app, the Keychain write (tests may not touch it), ⌘O/sidebar by eye
- [x] Remote relay, Phase 2: Settings › Servers — paste the startup URL, token in the Keychain, live add/remove, state in the sidebar — [detail](remote-relay-phase2.md) — **one paste, one door, no relaunch** ([ADR-0021](../decisions/0021-servers-are-a-pasted-url-and-a-keychain-item.md)). Settings › Servers is `ServersSection`: one row per `[[servers]]` table with a dot and a word (`connected · N sessions` in the alive green, `refused` / `no token` / `reconnecting` in the accent, `disabled` grey), the URL, the last error as one `$` line, on | off, paste token (offered only where a token would change something), remove (confirmed), and the name as a field that renames on leaving it; under the rows a name and one paste field. `RelayServerTokens.parse` takes the line as the terminal printed it (label, ANSI, prompt, trailing punctuation), a bare URL, or a URL on the wrong path, and refuses no host, another scheme, or a token that is not a JWT; the name defaults to the host's first label. `RelayServerBook` is the one door for add / remove / enable / rename / paste from the window, `maxpane server add` and a hand edit alike: every change is a comment-keeping edit through `ConfigStore`, whose own `didChange` (also what `ConfigWatch` fires) reaches `reconcile`, which diffs the enabled entries against `RelayServers` and starts or stops one `RemoteSessionSource` each — so nothing waits for a relaunch, and the "connection test" is the new source's first `GET /api/sessions` on its own queue. A rename runs the core's new `rename_relay_server` (panes and `old:path` cwd tags, never a manual tag) before the file write so the lanes re-attach under the new name; a removed server's panes re-attach through the "not configured" transport and keep their place with the grey banner. A 401 empties the server's slice, so ⌘O and the sidebar carry nothing from it; a pasted token rebuilds the source under the same name. The token is a Keychain item for the host (removed with the server unless another shares the host) and the tests prove it through a dictionary store, never the login keychain. Sidebar: local groups first, then each enabled server as its own header (name; `TOKEN REFUSED` / `RECONNECTING`, else `N SESSIONS` or the blocked count, pulsing) above its project groups; refused is a header with no rows; disabled is not drawn; a click opens Settings on that row. relay-tty 1.23.0 reads `?token=` on `/ws/share` only (`ws-handler.ts:201`; `verifyWsAuth` is cookie-only), so every relaytty.com row carries the one-line notice. Verified against the real server through relaytty.com (`LiveServerBookTests`): a wrong token refused in one line, the pasted one connected with 1 session; the tunnel had died silently twice more and was restarted per the recipe. Render sheets of the section and the sidebar looked at in light and dark. Not driven in the running app: no instance was launched
- [x] Remote relay, Phase 3: start Claude Code on a remote server from ⌘O/⌘T/⌘D, BLOCKED works, remote projects gather — [detail](remote-relay-phase3.md) — **a line runs where the focused lane is, and the wrapper goes over the API** ([ADR-0022](../decisions/0022-a-remote-spawn-sends-the-wrapper-and-the-cwd-is-the-root.md)). `SessionSpawning` seam: `LocalSpawner` (the old `RelaySessionSpawner`, renamed; `TypedCommand` top-level) and `RemoteSpawner` (`POST /api/sessions` with the cookie, 201 is readiness, then `GET /api/sessions/:id`; completes on main, never blocks it). The body is never the program's name: `{"command":"$SHELL","args":["-li","-c","<line>\nexit $?"]}` (argv is `'prog' 'arg'; exit $?`), a bare shell is `$SHELL` with no args and gets `--login` from the server, `cwd` omitted when unknown so the server answers with its home. `SpawnPlace.resolve` is the one cwd rule: `@name` > a usable remembered place > the focused lane's server and directory; ⌘T/⌘D take the focused pane's place. ⌘O grammar `@server line` / `@local line`, the LAUNCH row says `server:dir`, an unknown `@word` is left as typed and says so, the ⌘/ sheet lists it; recents remember the server as `host:path` (no migration) and are dropped while their server is not connected; a failed remote spawn reopens the picker with the line and one line naming the server and its error. `maxpane run` stays local. Project root stays `host:`+cwd itself, no git walk, with a Rust gather test. **On the real box through relaytty.com (`LiveRemoteSpawnTests`):** spawn in 209–319 ms, BLOCKED in the registry and on the sidebar group, answered over the WebSocket, lane closed and reattached, torn down; the control — the same program as `{"command": …}`, relay-tty's own `exec` path — is pid = pgid = sid and reads `idle` every time. **The BLOCKED proof used a stand-in** (a `cat` named `claude` behind a script printing `Do you want to proceed?`): the real Claude Code 2.1.276 on the box starts through the wrapper with `foregroundProcess: claude`, but its OAuth token has expired (`API Error: 401`), and its trust-folder and usage-credits dialogs match none of relay-tty's BLOCKED patterns (read `working`) — a classifier gap upstream. Not driven: the running app, so ⌘O/⌘T/⌘D by hand, the reopened picker and the ⌘/ sheet have not been seen on a screen; `build/verify.app` assembled, not launched
- [x] Remote presence: a server chip marks remote sessions everywhere, and a dead server shows at once on its header, its rows, its lanes and its tiles — [detail](remote-presence.md)
- [x] Collapsing a sidebar group (directory or server) hides its lanes from the strip and gallery; expanding brings them back — [detail](sidebar-collapse-hides-lanes.md) — a sibling of the gather filter in the core (lane ids, because a sidebar group is a cwd and not a tag), persisted in the ledger; hidden is not closed; focus landing in a folded group opens it; ADR-0024
- [x] A server has a colour, picked from a submenu on its sidebar header; a colour square marks its sessions, and the name stays only where there is no other context — [detail](server-colour.md)
- [x] ⌘V of a file copied in Finder pastes its full path (today: only the name), double-quoted when it has spaces or shell characters; several files space-separated — [detail](paste-file-paths.md)
- [x] A risky paste asks first (several lines, tabs, or a lot of bytes) in a sheet with Paste / One Line / Cancel; every paste is sent in ≤1 KB chunks — [detail](paste-confirm-sheet.md) — **Asks in a sheet over the pane; everything a pane sends is paced pieces.** `TerminalPaste.asksFirst` (interior line ending, tab, more than `paste_confirm_bytes`; measured on the bytes that go out) holds a paste in `PasteAskSheet`: counts, a line per reason, eight lines as sent with control pictures. ↩ and Esc cancel; **P** Paste, **O** One Line (`oneLine`, shell continuations joined), **T** Tabs to Spaces (`tabsToSpaces`, `paste_tab_width`). `Command.pasteWithoutAsking`, ⌥⌘V, in a new Edit `MenuSection`, by the responder chain (`pasteIntoTerminalPaneWithoutAsking:`); `paste(_:asking:)` is the door. Four settings, all live through `liveConfig`. `InputChunks` (in RelayClient, so the bench links it) cuts ≤1 000 bytes on UTF-8 boundaries; `PacedInput` in the attachment adapter sends one piece per 5 ms from one FIFO, below `PendingInput`, so held input flushes through it too. 5 ms is measured against a pty-host built from the commit before relay-tty 1.23's fix: unpaced pieces lost two thirds of 64 KB, 1–2 ms failed a byte-at-a-time reader, 5 ms delivered 256 KB. 1 MB: 1 049 messages, 7.1 s local, 7.2 s through the tunnel, sha256 equal. ADR-0026.
- [x] ⌘V with only an image on the clipboard saves it and pastes the path; in a remote lane it uploads through the relay API and pastes the remote path — [detail](paste-clipboard-image.md)
- [x] OSC 52: programs can set the clipboard (allowed, shown), and must ask to read it; find out what happens today — [detail](terminal-osc52.md)
- [x] Pasted commands are tidied: smart quotes and dashes straightened, a copied `$ ` prompt removed, stray whitespace trimmed, and the pane says so — [detail](paste-text-hygiene.md)
- [x] Middle-click pastes in a terminal: the pane's selection first, else the clipboard; the program keeps the click when it has mouse reporting on — [detail](terminal-middle-click-paste.md)
- [x] Edit › Paste Special: Paste Escaped, as Base64 / decoded, a File as Base64 heredoc, and Paste Slowly — [detail](paste-special.md)
- [x] Paste history picker: what was pasted into and copied out of terminals, never the system clipboard, never concealed or private content — [detail](paste-history.md)
- [x] Advanced Paste: one sheet combining the landed transforms plus a regex, with a preview of the exact bytes (last; rejects itself if too few transforms exist) — [detail](paste-advanced-dialog.md)
- [x] Copying out of a terminal: trimmed and unwrapped by default, Copy with Styles, and a keyboard copy mode — [detail](terminal-copy-side.md)
- [x] YouTube in a web pane: the first Play gives no sound until the video is clicked again — find the cause (focus click, autoplay policy, blocker, injected script), fix it, keep gesture-less autoplay quiet — [detail](web-first-play-silent.md)
- [x] Audio indicators: a sidebar row's status square becomes a speaker while its pane plays sound (click mutes, right-click opens a volume slider); the same speaker on lane header, gallery tile and status bar; Mute Pane / Others / All; `maxpane mute|volume` — [detail](web-audio-indicators.md)
- [x] A folded sidebar header rolls up the activity under it: BLOCKED (pulsing), DONE (orange), WORKING (green mark), not just the hidden count; a click on the state goes to that session — [detail](folded-header-activity.md)
- [x] The version in the corner reads `v0.6.1+40` on an unreleased build (count from the bundled CHANGELOG), a tooltip says what it was built from, and a click opens the changelog — [detail](version-from-changelog.md)
- [-] ⌃V in a remote lane with an image-only clipboard does what ⌘V does (upload through the relay server, paste the remote path); every other ⌃V stays the raw byte — [detail](ctrl-v-image-remote.md)
