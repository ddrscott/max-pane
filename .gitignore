target/
# The static archive gen-bindings.sh stages for the Swift linker.
/swift/MaxPaneCore/lib/
.build/
.DS_Store
*.xcodeproj
/build/
/dist/
/spikes/**/.build/
/spikes/**/target/
*.app/

# Agent worktrees, created by the harness for isolated builders.
.claude/

# Apple's provisioning profile for the passkeys capability, if the owner has one.
# It lives in packaging/ on the release Mac only; see the README.
*.provisionprofile
